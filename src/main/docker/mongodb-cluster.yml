version: '2'
services:
    epl-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb epl-mongodb-config
    epl-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    epl-mongodb-config:
        image: mongo:3.6.3
        container_name: epl-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
